13.4.4
указатель может быть не только скаляром, но и массивом, более того многомерным массивом
возможность исп массив указателя реализается повсеместно, а исключительная возможность определяется для ф-ции мэин
чаше всего мэин не имеет аргументов но это не значит что программа не может получить данные из вне
эта задача решается с помошбю массива указателей
си позволяет передать в ф-цию мэин любое к-во параметров
имеет ли
int main(int arg, char *argv[])
первый агм в мэин является значение определяющее количество передаваемых параметров
вторым является массив указателей
передаваемые параметры записываются в командной строке после имени программы(имя программы явл нулевым значением для указателя)
int main(int argc, char*argv[])
...
if(argc<2)
{
cout << "\nYou need to enter an -S, -s, -L, -l"
<< " and at least one integer value" << endl;
exit(1);
}//проверка аргументов слева направо, далее следует ключ "-"
while(--argc>0 && (*++argv)[0]=='-')
{
...
exit(1);
}
}
}
if(argc==0)
{ 
cout << "Pliase enter at least one number\n";
exit(1);
}
...
while(argc--)//сколько чисел задано, столько и сравнивать
{
...
}
return(0);
}


скелет программы осуществляет сравнение численных значений
сравниваемые значения ... командной строки

пример:
c:<l a[.exe] 3 -s
все передаваемые аргументы ф-ции мэин разделяются пустым символом

13.4.5


указатель позволяет манипулировать не только операциями на данных, а и адресами которые хранят эти данные
такой подход позволяет осуществлять управление памятью

переписать код где
#define IMAXELEMENTS 3
void voutput(int**ppiresult_a, int**ppiresult_b, int**ppiresult_c);
